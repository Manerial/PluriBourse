# Deferred Work

## Deferred from: code review of 1-13-navigation-carrousel-benevole (2026-08-24)

- **Pas de test pour la phase `CLOSED` dans la sidebar bénévole** — `app-layout.component.html:190-248` — retombe sur la seule entrée Catalogue, comme le cas `PREPARATION` déjà testé. Comportement présumé correct, juste non vérifié pour cette valeur d'enum précise. Risque faible : un bénévole n'atteint quasiment jamais les écrans d'une édition Clôturée.
- **AC1 (liste complète des routes `/account`, `/printer-selection`, `/404`) non testée explicitement** — `app-layout.component.spec.ts` — vrai structurellement (le rail vit dans le wrapper `AppLayoutComponent` partagé, donc garanti présent), mais aucun test n'affirme explicitement sa présence sur chacune de ces routes.
- **Test "does not render any admin-only links" ne vérifie que le préfixe `routerLink`** — `app-layout.component.spec.ts:305-310` — ne couvre pas d'autres affordances admin-only potentielles (boutons, contrôles d'édition). Théorique : rien d'autre n'existe actuellement dans le `.sidebar` partagé à part des liens.
- **Pas de test d'exposition du tooltip au focus clavier (pas seulement au survol) sur les items du rail replié** — `app-layout.component.html` — lacune pré-existante, partagée avec les items déjà repliés de la sidebar admin, non introduite par cette story.

## Deferred from: code review of 6-2-consultation-catalogue-edition-archivee (2026-08-21)

- **Jeton de direction du tri jamais validé** — `ArchivedItemService.java:406-413` (`validateSort`) — un `sort=name,sideways` (ou toute direction malformée) n'est pas rejeté, seul le nom de champ est vérifié contre la whitelist. Reproduit fidèlement `ItemCatalogService.validateSort`, copié tel quel selon les Dev Notes de la story — pré-existant, non introduit par cette story.
- **Pas d'annulation des requêtes HTTP lors de changements rapides de filtre/tri/page** — `archived-catalog.component.ts` — pas de `switchMap`, seul un compteur `requestSequence` écarte les réponses obsolètes côté client ; chaque frappe/clic déclenche quand même un appel réseau complet. Hérité tel quel du pattern `ItemCatalogComponent`, explicitement demandé par les Dev Notes de la story.
- **Chargement complet en mémoire de tous les articles archivés d'une édition avant filtrage/pagination** — `ArchivedItemService.java:383` (`findAllByEditionId` puis filtrage Java) — réutilisation délibérée du pattern JPageFlow de `ItemCatalogService` (consistance actée dans les Dev Notes). Risque de passage à l'échelle réel à surveiller : contrairement au catalogue actif (remis à zéro à chaque édition), l'archive s'accumule sur plusieurs années.
- **`toLowerCase()` sans `Locale` explicite dans le filtre `name`** — `ArchivedItemService.java:420` (`matches`) — risque de bug de casse type "Turkish-I" selon la locale JVM. Hérité tel quel du pattern existant `ItemCatalogService.getCatalog`.

## Deferred from: code review of 2-7-cloture-de-ledition-archivage-de-ledition (2026-08-20)

- **FR-096 bypass via the pre-existing `/phase/advance` endpoint** — `EditionService.java:137-145` (`advancePhase`/`computeNextPhase`) — a direct `POST_SALE → CLOSED` transition through the generic phase-advance endpoint completely bypasses `EditionClosingService`'s atomic auto-Non-réclamé logic (FR-096); confirmed live and demonstrated by this story's own `EditionArchivingIT.advance_to_closed()` (Order 7), which closes via the old endpoint leaving seller "Bob" permanently UNSETTLED with no `Settlement` row. Fixing this (blocking the direct transition, unconditionally or only when unsettled sellers remain) breaks 7 pre-existing IT files across 4 already-`done` stories that use `/phase/advance` as scaffolding to reach Clôturée without ever settling sellers: `PhaseTransitionIT` (Story 2.2, ×2), `ItemCatalogIT`, `SettlementIT` (Story 5.1), `SettlementReportPrintingIT`, `EditionReportPrintingIT` (Story 5.4), `ReportExportIT` — plus this story's own `EditionArchivingIT`. Needs a dedicated follow-up story, not a code-review patch.

## Résolutions constatées lors de l'audit du 2026-06-24

Les items suivants ont été résolus dans des stories ultérieures et ne nécessitent plus d'action :

- ~~**`app.html` scaffold Angular avec strings codées en dur**~~ → ✅ Résolu Story 1.7 : `app.html` = `<router-outlet />` uniquement
- ~~**`authGuard` ne vérifie pas `forcePasswordChange`**~~ → ✅ Résolu Story 1.2/1.3 : `auth.guard.ts` contient le check
- ~~**`adminGuard` redirige VOLUNTEER vers `/login`**~~ → ✅ Résolu Story 1.7 : redirige vers `/volunteer` si authentifié
- ~~**`index.html <title>` hardcodé `PluribourseFrontend`**~~ → ✅ Résolu Story 1.7 : `<title>PluriBourse</title>` (TitleStrategy dynamique = nouveau defer existant)
- ~~**`app.spec.ts` test scaffold cassera en Story 1.7**~~ → ✅ Résolu Story 1.7 : spec mis à jour avec tests router-outlet

## Correctifs appliqués hors story — 2026-06-24

- ~~**`proxy.conf.json` expose tous `/actuator/*`**~~ → ✅ Corrigé : path restreint à `/actuator/health`
- ~~**`ngx-translate` : pas de langue de fallback explicite**~~ → ✅ Corrigé : `defaultLang: 'fr'` ajouté dans `app.config.ts`
- ~~**`aria-label="Current phase"` masque la valeur de phase**~~ → ✅ Corrigé : `[attr.aria-label]` retiré du phase-chip ; texte visible = nom accessible

## Résolutions constatées lors de l'audit du 2026-08-19

44 items ont été vérifiés un par un contre l'état actuel du code par 4 agents en lecture seule (un par tranche chronologique du fichier) et retirés ci-dessous : soit corrigés par une story ultérieure sans que ce fichier ait été mis à jour, soit devenus obsolètes (route, fichier ou fonctionnalité disparue). Le détail de chaque vérification (fichier relu, preuve, verdict) a été communiqué à l'utilisateur au moment de l'audit et n'est pas reproduit ici pour garder ce fichier lisible.

## Deferred from: code review of 5-6-impression-groupee-des-bilans-de-vente-admin (2026-08-19)

- **Transaction `@Transactional(readOnly=true)` englobe la génération PDF + soumission file (travail non-DB) pour jusqu'à ~100 vendeurs** — `SettlementReportPrintService.java:80-111` — même patron déjà présent sur `printReport` (single-seller), amplifié par la boucle sur toute l'édition (NFR-001 ~100 vendeurs). Fix systémique hors périmètre de cette story.
- **Aucun verrou serveur contre une double soumission concurrente** — `SettlementReportPrintService.java` / `AdminSettlementController.java` — deux onglets/sessions admin peuvent ré-enfiler l'intégralité des bilans ; limitation déjà présente sur le flux d'impression individuel, pas introduite par cette story.
- **Job en échec (course `PrinterNotFoundException`) invisible pour l'admin au-delà du compteur `failedCount`** — `SettlementReportPrintService.java:105-107` — décision de conception explicitement actée dans la story (AC 5 ne requiert que le compteur, pas l'identification du vendeur en échec).
- **Spinner du bouton groupé sans `aria-label` de repli ni `aria-live` pendant l'envoi** — `settlement-list.component.html:14-16` — reproduit fidèlement le patron existant de `printer-list.component.html`, non spécifique à cette story.
- **Paramètre `filter` sans validation explicite/valeur par défaut** — `AdminSettlementController.java:29-31` — une valeur invalide passe par le handler par défaut de Spring (déjà RFC7807 via `ResponseEntityExceptionHandler`, juste sans le `type` URI custom de l'appli). Première occurrence de ce pattern (`@RequestParam` enum), non atteignable via l'UI normale.
- **422 `settlement-not-allowed` (changement de phase pendant que la page est ouverte) tombe dans le message d'erreur générique** — `settlement-list.component.ts:207-215` — même lacune déjà présente sur `printReport()` individuel.
- **Suspension de la file d'impression en cours de boucle comptée comme "succeeded"** — `SettlementReportPrintService.java:100-109` — `PrinterQueueHandle.submit()` ne revérifie pas `suspended` par itération. Cohérent avec la sémantique "mis en file" de l'AC 4 (pas "imprimé") ; le suivi de l'impression réelle relève de la page `/admin/print-queue` existante (Story 3.7).
- **Filtre sans correspondance au moment de la requête → toast succès "0 bilans mis en file"** — `SettlementReportPrintService.java:94` / `settlement-list.component.html:10` — potentiellement trompeur, mais le bouton est déjà désactivé côté client quand la liste filtrée est vide ; seule une course étroite (données changées entre chargement de page et clic) ou un appel API direct l'atteint.

## Deferred from: code review of 1-1-mise-en-place-du-squelette-de-projet-baseline-docker-compose (2026-06-15)

- **Hash BCrypt admin commis dans Liquibase** — `001-core-schema.xml:34` — Intentionnel (`force_password_change=true`), Story 1.2 implémentera le reset obligatoire au premier login. Risque résiduel : plaintext documenté dans le commentaire adjacent.
- **Spring Session H2 : slice tests sans Liquibase** — `002-spring-session.xml` — Les `@WebMvcTest` ou slice tests qui désactivent Liquibase échoueront silencieusement sur `SPRING_SESSION`. Aucun test affecté actuellement. À surveiller si des slice tests sont introduits.
- **Colonne `role` en `VARCHAR(20)` sans contrainte `CHECK`** — `001-core-schema.xml:19` — La couche JPA enforcer les valeurs valides via `@Enumerated(STRING)` quand l'entité `User` sera créée. Pas de risque tant qu'aucun accès DB direct n'existe.
- **Colonne `preferred_language` sans contrainte de valeurs** — `001-core-schema.xml:23` — Scope Story 1.6 (i18n complet). À adresser lors de la mise en place de la préférence de langue utilisateur.

## Deferred from: code review of 1-2-authentification-spring-security-controle-dacces-base-sur-les-roles (2026-06-16)

- **Pas de rate limiting sur `/login`** — protection brute-force à prévoir (Spring Security `LockingUserDetailsService` ou Bucket4j). Epic de hardening sécurité.
- **Colonne `preferred_language` sans contrainte CHECK DB** — valeurs invalides → `IllegalArgumentException` JPA. À ajouter en hardening schéma.
- **Changement de mot de passe concurrent depuis deux onglets** — second onglet garde contexte de sécurité périmé. Secondaire au Patch stale-forcePasswordChange.

## Deferred from: 2nd code review pass of 1-2-authentification-spring-security-controle-dacces-base-sur-les-roles (2026-06-16)

- ~~**`PluriBourseUserDetails` manque de `isEnabled`/`isAccountNonLocked`**~~ → ✅ Résolu Story 1.3 : `User.enabled` + `PluriBourseUserDetails.isEnabled()` implémentés.
- ~~**`UserService.changePassword` lève `IllegalArgumentException` → 500**~~ → ✅ Résolu : passe désormais par `getUser()` / `UserNotFoundException` → 404.
- **Changement de mot de passe n'invalide pas les autres sessions actives** — les sessions parallèles (autre navigateur/onglet) restent valides jusqu'à leur expiration (P1D). À adresser avec Spring Session registry si le besoin d'invalidation immédiate émerge. **Tenté puis annulé lors de l'audit du 2026-08-19** : brancher `sessionInvalidationService.invalidateSessionsFor()` directement dans `UserService.changePassword()` invaliderait aussi la session courante avant que `AuthController.changePassword()` n'appelle `securityContextHelper.refreshSessionPrincipal()` sur cette même session — comportement dépendant du timing exact de ré-écriture de la session par le filtre Spring Session, non vérifiable sans test dédié. Une correction propre nécessite soit d'exclure la session courante de l'invalidation (id de session à passer en plus du username), soit d'appeler l'invalidation après le rafraîchissement du principal. Pas un fix d'une ligne comme supposé initialement.

## Deferred from: re-run code review of 1-1 (2026-06-16)

- **`handleMethodArgumentNotValid` ignore les `getGlobalErrors()`** — `GlobalExceptionHandler.java` — Les erreurs de validateurs de classe (`@ScriptAssert`, `Validator` custom) ne sont pas incluses dans le `detail` RFC 7807. Sans impact tant qu'aucun validateur objet n'existe. À corriger quand des `@ScriptAssert` ou `Validator` custom seront introduits.
- **`logback-spring.xml` : enforcement PII structurel absent** — `src/main/resources/logback-spring.xml` — La politique no-PII est documentée en commentaire. Aucun `TurboFilter` ou masquage pattern n'est en place. Acceptable pour le skeleton ; à adresser si le besoin de garantie formelle émerge.

## Deferred from: code review of 1-5-configuration-de-linstance-page-de-parametres-admin (2026-06-23)

- **Pas de cache sur `findConfig()`** — `InstanceConfigService.java` — Chaque appel à `getDefaultDocumentLanguage()` et `getDefaultCommissionRate()` déclenche une requête DB séparée. Les appels depuis Story 2.1 dans la même transaction ouvriront des transactions imbriquées sans partage du cache JPA first-level. À adresser avec `@Cacheable` si les performances s'avèrent insuffisantes.
- ~~**Pas de bouton "Réessayer" après échec de chargement**~~ → ✅ Résolu 2026-08-19 : bouton `retry()` ajouté sous le message d'erreur, réutilise `loadConfig()`.

## Deferred from: code review of 1-5-configuration-de-linstance-page-de-parametres-admin 2ème passe (2026-06-23)

- ~~**`InstanceConfigMapper` NPE si `defaultDocumentLanguage` null**~~ → ✅ Résolu : même fix que ci-dessus (DTO typé enum), plus d'appel `.name()` sur un champ nullable.
- **Order(5) n'assert pas les champs non modifiés après PUT** — `InstanceConfigIT.java:Order(5)` — Le test ne vérifie que `defaultCommissionRate` après la mise à jour. Un bug de partial-write (autres champs remis à zéro) ne serait pas détecté. Les autres champs sont couverts par leurs propres tests dédiés.
- **État intermédiaire `isSaving()` non testé dans le spec** — `admin-settings.component.spec.ts` — Les tests ne vérifient que la valeur finale de `isSaving()`. Si `isSaving.set(true)` était supprimé d'`onSubmit()`, les tests passeraient quand même.

## Deferred from: code review of 1-6-preference-de-langue-utilisateur-infrastructure-i18n (2026-06-24)

- **Race condition sur premier login concurrent** — `LoginSuccessHandler.java` — Deux logins simultanés du même nouvel utilisateur lisent tous deux `languageInitialized=false` dans le principal d'authentification et appellent `initializeLanguage()` ; le second écrase le premier. Probabilité négligeable dans le cas d'usage cible (bourse).
- **NPE dans `LanguagePreferenceIT` Order(11) si Order(9) échoue** — `LanguagePreferenceIT.java` — `frUserUsername` est null si Order(9) ne s'exécute pas (filtre de test IDE). Limitation inhérente au pattern de scénario ordonné documenté dans CLAUDE.md.
- ~~**Bannière `saveSuccess` persistante après changement de select sans resoumission**~~ → ✅ Résolu : la bannière a été remplacée par `ToastService.showSuccess()` (auto-dismiss).
- **Admin seedé perd sa langue FR au 1er login post-migration 006** — `006-user-language-initialized.xml` — Migration ajoute `language_initialized = false` sur tous les utilisateurs existants. L'admin reçoit la langue du navigateur au 1er login. Comportement intentionnel selon la spec ; pas de backfill prévu.
- **`restoreSession()` ne restaure pas la langue sur le chemin 403 `forcePasswordChange`** — `pluribourse-frontend/src/app/services/auth.service.ts:61` — La page `/change-password` s'affiche en 'en' quelle que soit la préférence langue de l'utilisateur. Un fix propre nécessiterait d'inclure `preferredLanguage` dans le body de la réponse 403, ce qui est hors scope de cette story.

## Deferred from: code review of 1-3-gestion-des-comptes-benevoles (2026-06-22)

- **URL d'erreur `account-disabled` dupliquée en magic string Java + TypeScript** — `LoginFailureHandler.java` et `login.component.ts` — `"https://pluribourse/errors/account-disabled"` hardcodé dans les deux couches sans constante partagée. Une divergence silencieuse casse la détection côté frontend. Cross-langage, pas de solution compile-time. Risque faible à court terme.

## Deferred from: code review of 1-7-systeme-de-design-angular-material-mise-en-page-applicative — Pass 2 (2026-06-24)

- **`authInterceptor` coupe la session sur tout 403** — `pluribourse-frontend/src/app/core/interceptors/auth.interceptor.ts` — Le 403 d'une ressource admin protégée (feature flag, permission granulaire) déconnecte l'utilisateur comme un 403 d'expiration de session. Pré-existant.
- ~~**Hard reload `forcePasswordChange=true` bloque `/change-password`**~~ → ✅ Résolu : sentinelle posée sur `currentUser` dans le catch 403 de `restoreSession()`.

## Deferred from: code review of 1-7-systeme-de-design-angular-material-mise-en-page-applicative (2026-06-24)

- ~~**"Édition active" section sans liens de navigation**~~ → ✅ Résolu Epic 2 : lien `routerLink="/admin/editions"` ajouté.
- **Topbar `position: sticky` vs spec "fixed"** — `app-layout.component.scss:27` — La spec dit "fixed topbar" mais l'implémentation utilise `sticky`. Comportement équivalent dans le layout CSS grid actuel (`.app-shell` sans overflow, scroll au niveau document). À surveiller si la structure du shell évolue.
- **`forcePasswordChange: undefined` contourne le guard** — `auth.guard.ts` — Si l'API retourne un 200 sans le champ `forcePasswordChange`, le guard ne redirige pas vers `/change-password`. Pré-existant, non introduit par cette story.
- ~~**Race condition de restauration de session**~~ → ✅ Résolu : `provideAppInitializer` bloque le bootstrap jusqu'à la résolution de `restoreSession()`.
- **`logout()` sans gestion d'erreur / flash cosmétique** — `app-layout.component.ts:22-24` — `AuthService.logout()` utilise un `finally` qui navigue toujours vers `/login`. Un échec POST n'empêche pas la déconnexion, mais l'absence de `try/catch` dans le composant supprime tout feedback d'erreur visible. Flash d'un frame : role-badge passe à "volunteer" avant la navigation.
- **Google Fonts sans attributs SRI ni crossorigin** — `index.html:9-10` — Deux `<link>` Google Fonts sans `integrity` ni `crossorigin`. Risque de sécurité faible pour un outil auto-hébergé ; les polices pourraient être intégrées localement en hardening.
- **Double source CSS (`mat.theme()` + `:root`) fragile aux mises à jour** — `styles.scss` — Angular Material M3 génère ses variables `--mat-sys-*` via `mat.theme()`, puis `:root` les écrase immédiatement. Les variables émises par les composants Material dans leur propre portée (`:host`) peuvent re-overrider ces valeurs. À surveiller lors des upgrades Angular Material.
- **`!important` sur `.sidebar__item--active`** — `app-layout.component.scss` — `routerLinkActive` injecte la classe sur l'élément ; `!important` est nécessaire uniquement si des styles Material conflictuels interfèrent. À retirer si plus nécessaire lors d'une revue CSS.
- **Tokens CSS non utilisés définis dans `:root`** — `styles.scss` — `--pb-primary-hover` reste totalement inutilisé (`--pb-success-container`/`--pb-on-success-container` sont désormais utilisés par les badges, à retirer de la liste). Token intentionnel pour usage futur ; à supprimer si jamais implémenté dans un composant.
- **Pas de `TitleStrategy` pour les routes enfants** — Chaque page affiche "PluriBourse" dans l'onglet navigateur. À adresser avec `TitleStrategy` + clés i18n pour améliorer la lisibilité de l'historique et les annonces de changement de page pour les lecteurs d'écran.
- **Pas de breakpoint responsive pour le sidebar** — `app-layout.component.scss` — `has-sidebar` = `200px 1fr` sans `min-width` sur la colonne content. La story est explicitement desktop-only (v1). À adresser en Epic de responsive design.

## Deferred from: code review of 1-10-ameliorations-ux-mots-de-passe (2026-06-26)

- **Shared `submitting` signal creates table-wide lock on all reset buttons** — `user-list.component.ts` — While a password reset is in flight, all other reset buttons in the user table are disabled. The old inline form only gated its own submit button. Acceptable for single-user-at-a-time admin workflows; to refactor to per-row state if concurrent admin scenarios are introduced.

## Deferred from: code review of 1-9-guide-dinstallation (2026-06-26)

- **Promesse "aucune connaissance technique requise" partiellement contredite** — `GUIDE_INSTALLATION.md` introduction — Tension inhérente entre la promesse d'accessibilité totale et les étapes en terminal nécessaires. Non actionnable sans réécriture du positionnement du guide.

## Deferred from: code review of 1-8-composants-ui-partagees-boites-de-dialogue-notifications-accessibilite (2026-06-25)

- ~~**confirm-dialog — fermeture backdrop émet `undefined`**~~ → ✅ Résolu en pratique : les 3 appelants réels (`edition-list`, `phase-control`, `user-list`) utilisent `if (!confirmed)`, qui traite `undefined` comme falsy.
- **`ToastService` — pas de nettoyage du timer sur destroy** — `toast.service.ts` — Aucun hook `DestroyRef.onDestroy`. Risque faible pour un service root ; les tests utilisent `vi.useRealTimers()` en afterEach. À adresser si des instances non-root sont introduites.
- **Sidebar — focus ring corail potentiellement invisible sur fond sombre** — `app-layout.component.scss` — La règle `a:focus-visible` globale s'applique aux liens sidebar, mais le fond sombre (`--mat-sys-on-primary`) peut masquer l'outline corail. À vérifier manuellement.
- ~~**Auth — erreur affichée via `<app-notification-inline>` au lieu de `<p class="auth-card__error">`**~~ → ✅ Résolu : la classe `.auth-card__error` a été entièrement supprimée du CSS, plus de code mort.
- **T9.5 — `ChangePasswordComponent` : logique hors périmètre spec** — `change-password.component.ts` — Injection `ToastService`/`TranslateService` et toast de succès ajoutés hors du scope T9.5. Amélioration intentionnelle mais non spécifiée.
- **`change-password` — `showSuccess()` avant `await router.navigate()`** — `change-password.component.ts:37` — Le toast succès s'affiche avant la fin de la navigation. Risque négligeable (routes statiques, pas de lazy loading sur ce chemin).

## Deferred from: code review of 2-1-crud-dedition-configuration-du-taux-de-commission (2026-06-28)

- **Race condition création concurrente d'édition** — `EditionService.java:createEdition()` — check-then-insert sans contrainte DB unique partielle sur phases actives ; deux POST simultanés peuvent créer deux éditions actives. Fix: contrainte `UNIQUE` partielle sur la colonne `phase` limitée aux phases actives, ou isolation `SERIALIZABLE`. Low risk pour une plateforme mono-admin.
- ~~**Assertion BigDecimal style**~~ → ✅ Résolu : `isEqualByComparingTo` utilisé partout dans `EditionManagementIT.java`.
- ~~**Race condition double-clic Delete**~~ → ✅ Résolu : guard `isConfirmingDelete` ajouté dans `edition-list.component.ts`.
- **`createdEditionId` null en cascade** — `EditionManagementIT.java` — si `@Order(4)` échoue, les tests `@Order(5)` à `@Order(16)` génèrent des NPE ou URL `/api/admin/editions/null/...` masquant la vraie cause. Pre-existing pattern des tests IT ordonnés.

## Deferred from: code review of 2-3-blocage-benevoles-sans-edition-active (2026-06-30)

- ~~**`ACTIVE_PHASES` dupliqué dans `LoginSuccessHandler` et `EditionService`**~~ → ✅ Résolu : refactoré en constante partagée `PhaseType.ACTIVE`.
- **`EditionManagementIT @Order(9)` envoie `null` commissionRate pour contourner le frozen-rate check** — `EditionManagementIT.java:160` — La valeur précédente `new BigDecimal("15.00")` testait l'envoi du même taux en DEPOSIT. La correction à `null` évite le check mais ne couvre plus le cas "même valeur que le taux gelé". À investiguer si `EditionService` doit accepter ou rejeter une mise à jour idempotente du taux en DEPOSIT.
- **HTTP 422 pour auto-protection admin (disable/change-password/delete self)** — `UserManagementIT.java:158,197,293` — RFC 9110 définit 422 pour des erreurs sémantiques sur le corps de la requête, pas pour des contraintes de règle métier (l'admin ne peut pas se désactiver lui-même). 403 Forbidden ou 409 Conflict seraient plus corrects. Comportement pré-existant en production, non introduit par Story 2.3.

## Deferred from: code review of 2-2-controle-du-cycle-de-phases-boites-de-dialogue-de-confirmation (2026-06-29)

- **Async callback écrit sur les signals après destruction du composant** — `phase-control.component.ts` — `takeUntilDestroyed` annule la souscription Observable mais ne cancelle pas le `firstValueFrom` en vol. Si le composant est détruit pendant une transition, le `finally` block écrit sur les signals d'un composant détruit. Pas de crash en mode Signal Angular ; orphaned write sans conséquence visible. À adresser si des `ExpressionChangedAfterItHasBeenCheckedError` sont observées en dev.
- **`PhaseTransitionIT` cascade de null** — `PhaseTransitionIT.java` — `editionId` reste `null` si `@Order(1)` échoue. Les tests suivants génèrent des URLs `/api/admin/editions/null/...` avec des 404 trompeurs. Pre-existing pattern des tests IT ordonnés documenté dans CLAUDE.md. Sans `Assumptions.assumeTrue(editionId != null)` sur les autres méthodes, le diagnostic est bruyant.

## Deferred from: code review of 2-5-categories-de-ledition-correspondance-des-tables (2026-06-30)

- **`parseTableInput` silently drops table number 0** — `edition-categories.component.ts:103` — `filter(n => !isNaN(n) && n > 0)` drops 0 silently; user entering "0, 1, 2" sees `[1, 2]` with no warning. Spec says "1..N" so 0 is invalid, but silent drop is confusing UX. À adresser si des retours utilisateurs remontent.
- **Empty array PUT silently deletes all categories with no frontend guard** — `edition-categories.component.ts`, `EditionCategoryService.java` — If user removes all rows and saves, DELETE + empty INSERT runs without warning. Valid per bulk-replace spec; UX improvement only. À adresser si retours utilisateurs sur suppressions accidentelles.
- ~~**N+1 inserts in `persistCategories`**~~ → ✅ Résolu 2026-08-19 : remplacé par un unique `categoryRepository.saveAll()`.
- **Concurrent saves race condition** — `EditionCategoryService.java:206` — No pessimistic locking on delete+insert; simultaneous PUTs from two admin sessions could cause unique constraint violation (500 instead of graceful 422). Unlikely in single-admin context. À adresser avec `SERIALIZABLE` isolation ou `SELECT FOR UPDATE` si un contexte multi-admin émerge.
- **`copyFromEdition` with 0-category source silently clears all target categories** — `EditionCategoryService.java:215` — If the source edition has no categories, `deleteAllByEditionId` wipes the target with no user warning. Correct per bulk-replace contract; UI shows empty list as result. À adresser si retours utilisateurs sur comportement inattendu.

## Deferred from: code review of 2-6-notification-de-phase-en-temps-reel-via-sse (2026-07-01)

- **`SseService.phaseChanges()` creates a new `EventSource` per subscriber, no multicast/sharing** — `pluribourse-frontend/src/app/services/sse.service.ts` — Harmless with today's single consumer (`AppLayoutComponent`), but a footgun if a second consumer subscribes independently — it would open a second SSE connection to the same endpoint instead of sharing the stream. À adresser avec `share()`/`shareReplay()` si un second consommateur est introduit.
- **No fallback UI for an unmapped/future `PhaseType` value** — `pluribourse-frontend/src/app/layout/app-layout/app-layout.component.html` — The dynamic i18n key `'edition.phase.' + currentEdition()!.phase` has no fallback string; if backend and frontend `PhaseType` ever drift, the chip silently shows a raw untranslated key. Low-severity robustness gap, no such drift exists today.

## Deferred from: audit of deferred-work.md (2026-07-01)

- **Page `/login` affiche parfois `en`, parfois `fr` selon l'état de session résiduel** — `app.config.ts:19`, `auth.service.ts:63-79` — `provideTranslateService({ lang: 'en', fallbackLang: 'fr' })` fixe la langue de démarrage sur `en`, indépendamment de la langue du navigateur. `restoreSession()` ne bascule vers `preferredLanguage` que si une session valide est trouvée ; sur un chargement à froid sans session, `/login` reste toujours en `en`. Un flash en `fr` peut apparaître si une session encore valide traîne au moment du rendu de `/login` (avant redirection). Signalé par l'utilisateur comme comportement observé en usage réel. À investiguer : cause exacte du flash (timing de `restoreSession()` vs guard) avant de choisir un fix.

## Deferred from: code review of 3-1-gestion-des-profils-vendeurs (2026-07-02)

- ~~**AC7 « et tous ses articles » non vérifiable/implémentable pour l'instant**~~ → ✅ Résolu 2026-08-19 : Story 3.2 (`epics.md`) avait déjà tranché — blocage de la suppression si des articles sont encore enregistrés, jamais de cascade. AC7 de la Story 3.1, FR-021 et le Gherkin correspondant amendés dans `epics.md`/`3-1-gestion-des-profils-vendeurs.md` pour refléter ce comportement (documentation seule, aucun changement de code).
- **Aucune validation d'entrée sur le binding `FilterDto` de `/admin/sellers`** — `AdminSellerController.java:21` — page/size négatifs ou tri malformé peuvent produire une exception non gérée. Première utilisation de JPageFlow dans le code ; préoccupation transverse à l'intégration de la librairie, pas spécifique aux vendeurs. À traiter lors d'un futur durcissement si JPageFlow est réutilisé ailleurs.
- **Pas de validation de format côté serveur sur `phone`** — `SellerDto.java` — Seulement `@NotBlank`/`@Size(max=30)` ; AC6 exige seulement un champ non-vide, donc conforme à la lettre du spec, mais n'importe quel format est accepté. Amélioration future possible.
- **Gestion d'erreur générique côté client masque le message d'erreur serveur spécifique** — `seller-form.component.ts`, `seller-list.component.ts` — Correspond au pattern déjà utilisé dans `user-form.component.ts` ailleurs dans l'app ; pas une régression introduite par cette story, mais lacune UX récurrente à l'échelle de l'application. Audit 2026-08-19 : le chargement distingue désormais `no-active-edition`, mais `confirmDelete()` (`seller-list.component.ts:86-88`) reste générique.

## Deferred from: 2nd code review pass of 2-6-notification-de-phase-en-temps-reel-via-sse (2026-07-01)

- **`loadEdition()`'s `catchError(() => EMPTY)` silently swallows every non-401 HTTP error** — `pluribourse-frontend/src/app/services/current-edition.service.ts` — 5xx/network failures leave `currentEdition` frozen on stale data with no logging and no user-facing indication; only a subsequent SSE event (if one ever arrives) triggers a resync. Pre-existing risk-accepted trade-off from the prior review round. À adresser si un indicateur d'état dégradé (via `ToastContainerComponent`) s'avère nécessaire en usage réel.
- ~~**Stale in-flight `loadEdition()` resync can overwrite a newer optimistic SSE update**~~ → ✅ Durci 2026-07-01 : `current-edition.service.ts` — `latestRequestId` renommé `latestSequence` et désormais incrémenté aussi par la branche optimiste d'`updateFromEvent`, pas seulement par `loadEdition()`. Une résync restée en vol qui résout après un événement plus récent est donc ignorée. Couvert par un nouveau test dans `current-edition.service.spec.ts`. Ce durcissement n'était toutefois pas la cause du bug réellement signalé (voir ci-dessous).
- ✅ **Bug réel trouvé et corrigé 2026-07-01 — `SseEmitterRegistry.broadcast()` fermait la connexion après chaque événement** — `pluribourse-backend/src/main/java/org/pluribourse/shared/sse/SseEmitterRegistry.java` — `broadcast()` appelait `emitter.complete()` et vidait la liste des emitters après **chaque** envoi, forçant le client à rouvrir une connexion SSE. Si un second changement de phase survenait avant la fin de la reconnexion (ex. deux avancements de phase rapprochés), l'émetteur n'était pas encore réenregistré : l'événement partait dans le vide et le chip de phase (topbar) restait bloqué sur la phase précédente jusqu'à un rechargement complet de page. Symptôme signalé par l'utilisateur : "préparation → dépôt → vente affiche dépôt", confirmé reproductible seulement en enchaînant vite (ça fonctionnait en attendant un peu entre les deux, le temps que l'`EventSource` se reconnecte). Fix : les emitters restent ouverts et ne sont retirés que sur déconnexion réelle du client (`onCompletion`/`onTimeout`/`onError`). Couvert par un nouveau test E2E `PhaseTransitionIT.multiple_phase_changes_are_all_delivered_over_the_same_sse_connection`, vérifié en échec sans le correctif.

## Deferred from: code review of story-3-2-enregistrement-darticles-assignation-automatique-de-table — backend pass (2026-07-03)

- ~~**Aucune gestion d'exception ni test pour les conflits de verrouillage optimiste (`@Version` sur `Item`)**~~ → ✅ Résolu 2026-08-19 : `GlobalExceptionHandler` mappe désormais globalement `ObjectOptimisticLockingFailureException` → 409 RFC 7807 (`concurrent-modification`), couvre `Item` sans code spécifique. Volet « test » toujours absent (provoquer une vraie course de verrouillage optimiste nécessite deux threads réels type Testcontainers, cf. `SaleConcurrencyIT`/Story 4.4 — jugé disproportionné pour ce fix ponctuel).

## Deferred from: code review of 1-12-deconnexion-automatique-des-benevoles-desactives-ou-supprimes (2026-07-06)

- **TOCTOU race sur l'invalidation des sessions** — `pluribourse-backend/src/main/java/org/pluribourse/shared/security/SessionInvalidationService.java:14-18` — Une session ouverte pour le même principal entre l'appel `findByPrincipalName` (snapshot) et la boucle de suppression n'est pas révoquée. Limitation inhérente à l'approche find-then-delete ; fenêtre de course étroite (millisecondes), fermer ce cas nécessiterait un verrouillage plus large, hors scope de cette story.
- **`IntegrationTest` (classe de base des tests IT) ne câble jamais le vrai filtre de session Spring** — `pluribourse-backend/src/test/java/org/pluribourse/shared/IntegrationTest.java` — Le `mockMvc` partagé est construit via `webAppContextSetup(context).apply(springSecurity())` sans `springSessionRepositoryFilter`, donc `.session(mockHttpSession)` reste un objet en mémoire qui ne touche jamais la table JDBC `SPRING_SESSION`. Toutes les autres classes IT existantes (`UserManagementIT`, etc.) n'ont donc jamais réellement exercé la persistance de session JDBC. Story 1.12 a contourné le problème localement dans `VolunteerSessionRevocationIT` (MockMvc dédié avec le filtre réel) sans corriger la classe de base partagée. Gap systémique pré-existant, révélé mais non introduit par cette story.
- **Pas de log d'audit pour les révocations de session** — `pluribourse-backend/src/main/java/org/pluribourse/shared/security/SessionInvalidationService.java` — Aucune trace (même non-personnelle, ex. "N sessions révoquées pour l'utilisateur id X") n'est produite lors d'une révocation. Utile pour le débogage en production mais hors scope de cette story.

## Deferred from: code review of story-3.3 (2026-07-07)

- **Pas de contrainte niveau BDD garantissant `price` null ⇔ `lot_id` renseigné** — `pluribourse-backend/src/main/resources/db/changelog/015-lots.xml` — L'invariant "un item a soit un prix individuel non-null, soit un `lot_id` non-null, jamais les deux, jamais ni l'un ni l'autre" repose uniquement sur le code applicatif (`LotService`/`ItemService`), sans `CHECK` en base. Déféré : même périmètre restreint que le gap `DELETE` déjà accepté ci-dessous. À adresser si une story dédiée à la gestion des lots existants est créée.
- **`PUT /items/{id}` permet de fixer un prix non-null sur un article de lot sans le délier du lot** — `pluribourse-backend/src/main/java/org/pluribourse/item/service/ItemService.java:50-61` — `ItemService.update()` (inchangé) n'a aucune garde empêchant la modification d'un item membre d'un lot via `CreateItemDto` (qui exige un prix non-null), ce qui romprait l'invariant "un article de lot a `price = null`" dont dépend l'affichage de AC7. Déféré : même raisonnement que le gap déjà accepté sur `DELETE /items/{id}` (bouton Modifier masqué côté frontend pour ces lignes). À adresser avec le reste de la gestion des lots existants.

## Deferred from: code review of story-3.4 (2026-07-07)

- **File suspendue ne reprend jamais automatiquement, croissance illimitée de la `LinkedBlockingDeque`, attente active 200ms tant que suspendue** — `pluribourse-backend/src/main/java/org/pluribourse/print/service/PrinterQueueHandle.java:18,58-59,68` — Explicitement pré-scopé à la Story 3.7 (reprise/relance d'une file en erreur) : la story n'expose que les champs internes (`suspended`/`lastError`/`lastFailedJob`) pour leur future lecture/mutation. Audit 2026-08-19 : la reprise manuelle existe depuis la Story 3.7, mais aucune reprise automatique ni borne sur la file. Risque réel de croissance mémoire non bornée si la file reste suspendue longtemps.
- **Pas de hook d'arrêt (`@PreDestroy`) pour interrompre proprement les threads consommateurs daemon à l'arrêt de l'application** — `pluribourse-backend/src/main/java/org/pluribourse/print/service/PrinterQueueHandle.java` — Threads daemon, la JVM ne bloque pas à l'arrêt ; un job en cours est simplement abandonné sans trace. Aucune AC ne couvre ce cas ; amélioration future.
- **Pas d'endpoint update/delete ; le handle en mémoire garde un instantané `Printer` périmé et `registerPrinter()` est un no-op pour un id déjà enregistré, risque de thread orphelin si une imprimante était un jour supprimée** — `pluribourse-backend/src/main/java/org/pluribourse/print/service/PrintQueueService.java:54` — Le risque de thread orphelin à la suppression est résolu depuis la Story 3.8 (`unregisterPrinter()` appelle `stop()`) ; reste le problème dès qu'un endpoint update sera ajouté.
- **Aucun `PrinterConnectivityChecker` pour un `PrinterType` donné → `map.get()` retourne `null` → NPE avalée en tant que `lastError`** — `pluribourse-backend/src/main/java/org/pluribourse/print/service/PrintQueueService.java:75-77` — Actuellement inatteignable : exactement 2 checkers pour les 2 types existants (THERMAL, A4). Latent seulement si l'enum `PrinterType` grandit sans implémentation associée.
- **`registerPrinter` appelé avec un `Printer` transitoire (id `null`) provoquerait une NPE sur clé nulle de la `ConcurrentHashMap`** — `pluribourse-backend/src/main/java/org/pluribourse/print/service/PrintQueueService.java:54` — Actuellement inatteignable : les deux points d'appel (`create()`, `reloadFromDatabase()`) persistent toujours l'imprimante avant l'enregistrement.
- **Un second `PrinterConnectivityChecker` enregistré pour le même type ferait planter le démarrage (`Collectors.toMap` sans fonction de fusion)** — `pluribourse-backend/src/main/java/org/pluribourse/print/service/PrintQueueService.java:31-32` — Actuellement inatteignable (un seul checker par type) ; échec rapide et visible au démarrage si ça survenait un jour, pas une corruption silencieuse.
- **Tests sensibles au timing (attente active à intervalle fixe, hypothèses d'ordonnancement de threads dans le test `Order 8`)** — `pluribourse-backend/src/test/java/org/pluribourse/print/PrintInfrastructureIT.java:254-263` — Compromis accepté pour tester l'orchestration de threads réelle sans mock, cohérent avec la philosophie de test E2E du projet. Risque de flakiness sous charge CI à surveiller.
- **`registerPrinter()` s'exécute dans le bloc `@Transactional` de `create()` : connexion DB tenue ouverte pendant l'I/O bloquant de vérification de connectivité, risque de handle orphelin en cas de rollback futur** — `pluribourse-backend/src/main/java/org/pluribourse/print/service/PrinterService.java:27-37` — Non exploitable aujourd'hui, le corps de la méthode ne peut pas échouer après l'appel à `registerPrinter`. À surveiller si la méthode gagne en complexité.
- **Champs non pertinents pour le type choisi (`host`/`port` pour THERMAL, `serialPort`/`widthMm` pour A4) ni rejetés ni nettoyés, persistés silencieusement** — `pluribourse-backend/src/main/java/org/pluribourse/print/service/PrinterService.java:39-52` — Endpoint volontairement minimal dans cette story (juste assez pour enregistrer une imprimante testable) ; la richesse de validation relève naturellement du vrai formulaire admin de la Story 3.8.

## Deferred from: code review of story-3.5 (2026-07-20)

- **Duplication des clés i18n entre `messages.properties` et `messages_en.properties`** — `pluribourse-backend/src/main/resources/messages.properties` — Les 5 nouvelles clés `print.label.*` sont copiées à l'identique dans les deux fichiers. Rien n'empêche une divergence future si l'un des deux est modifié sans l'autre. Pré-existant : convention établie depuis la Story 1.6 (le fichier entier reproduit `messages_en.properties` comme fallback par défaut), pas spécifique à cette story.

## Deferred from: code review of story-3.6 (2026-07-21)

- **Soumission non atomique thermique+A4** — `pluribourse-backend/src/main/java/org/pluribourse/domain/item/service/DepositValidationService.java` — Si le job thermique est soumis avec succès puis que la soumission A4 échoue (rarissime, `PrinterQueueHandle.submit()` n'utilise qu'une `LinkedBlockingDeque` non bornée qui ne rejette jamais), le rouleau part sans garantie que le bordereau suive. Architecture `PrintQueueService` (Story 3.4) explicitement hors périmètre de modification pour cette story.
- **Scénario HTTP « les deux jobs partent »/« A4 indisponible alors que le thermique est disponible » jamais exercé de bout en bout** — `pluribourse-backend/src/test/java/org/pluribourse/domain/print/DepositSlipPrintingIT.java` — Limitation d'environnement : aucune imprimante THERMAL ne peut jamais passer sa vérification de connectivité réelle (pas de matériel série), et `validateDeposit()` vérifie le thermique avant l'A4. Même limitation déjà acceptée depuis la Story 3.5.
- **Aucun test avec des caractères hors CP1252 dans les noms vendeur/article** — `pluribourse-backend/src/main/java/org/pluribourse/domain/print/service/DepositSlipRenderer.java` — Risque de glyphes manquants/mutilés sur le bordereau PDF si un nom contient un caractère hors Windows-1252. Limitation systémique déjà acceptée pour tout le sous-système d'impression (`ThermalLabelRenderer` utilise aussi un charset Latin fixe, ISO-8859-15, sans support Unicode).
- **Pas de test dédié vérifiant le rejet par rôle sur `POST /{id}/deposit/slip/reprint`** — `pluribourse-backend/src/main/java/org/pluribourse/domain/seller/controller/SellerController.java` — Protection déjà assurée de façon centrale par `SecurityConfig` (règle générique authentifié+non-SELLER) ; même niveau de couverture que l'endpoint `deposit/validate` existant, non testé spécifiquement non plus.
- **Bouton « Réimprimer le bordereau » sans garde de phase côté client** — `pluribourse-frontend/src/app/features/volunteer/deposit/deposit-page.component.html` — Seul `items().length > 0` conditionne son affichage ; le backend est déjà l'autorité (422 vérifié par test), impact UX mineur seulement en cas de fenêtre de transition de phase (redirect réactif pas encore retombé).
- **Nombres non localisés dans le bordereau PDF (virgule française absente)** — `pluribourse-backend/src/main/java/org/pluribourse/domain/print/service/DepositSlipRenderer.java` — `commissionRate.toPlainString()`/`net.toPlainString()` produisent toujours un point décimal même en locale FR. Décision utilisateur 2026-07-21 : conserver tel quel, cohérent avec `ThermalLabelRenderer` (Story 3.5) qui utilise déjà `Locale.ROOT`/`%.2f` pour les prix quelle que soit la langue du document — ne pas diverger entre les deux documents imprimés pour la même donnée.

## Deferred from: code review of story-3.7 (2026-07-21)

- **Skeleton de chargement remplace toute la grille de cartes sur "Actualiser", pas seulement au chargement initial** — `pluribourse-frontend/src/app/features/admin/print-queue/print-queue-list.component.ts` — `load()` est appelée à la fois par `ngOnInit()` et par `refresh()` et bascule `isLoading` dans les deux cas, cachant les cartes déjà affichées derrière le skeleton lors d'un simple rafraîchissement manuel. Déviation du libellé littéral de la tâche ("pendant le chargement initial"), mais cohérent avec le pattern déjà utilisé par `EditionListComponent`/`UserListComponent` (mêmes composants liste admin, même comportement). Corriger uniquement cette page introduirait une incohérence UX avec le reste du module admin ; à traiter dans un refacto transverse si un pattern "rafraîchissement en arrière-plan" est souhaité pour toutes les pages liste.
- **Imports wildcard dans la nouvelle classe de test IT** — `pluribourse-backend/src/test/java/org/pluribourse/domain/print/PrintQueueDiagnosticsIT.java` — Convention reprise à l'identique de `PrintInfrastructureIT`, explicitement demandée par les Dev Notes de la story ("même style que `PrintInfrastructureIT`").

### Re-review (2026-07-21)

- **`canRetry` repose sur un invariant documenté en commentaire seulement** — `pluribourse-backend/src/main/java/org/pluribourse/domain/print/service/PrinterQueueHandle.java` — `suspended == true` implique `lastFailedJob != null` sans garantie du compilateur. Pattern hérité de la Story 3.4, étendu ici sans changement.

## Deferred from: code review of story-3.9 (2026-07-07)

- **Écritures concurrentes possibles sur la même `HttpSession` dans `selectPrinters()`** — `pluribourse-backend/src/main/java/org/pluribourse/print/service/PrinterSelectionService.java:55-57` — Deux `POST /printers/selection` interleavés sur la même session pourraient produire un état incohérent (`done=true` avec des ids dépareillés). Probabilité très faible : formulaire à soumission unique, bouton désactivé pendant `loading`. Pattern de session non synchronisée déjà accepté ailleurs dans le projet.

## Deferred from: code review of story-3.8 (2026-07-22)

- **Jobs en attente dans la file silencieusement perdus à la suppression d'une imprimante, sans trace ni avertissement admin** — `pluribourse-backend/src/main/java/org/pluribourse/domain/print/service/PrintQueueService.java` (`unregisterPrinter`), `PrinterQueueHandle.java` (`stop`) — `stop()` interrompt le thread consommateur mais rien ne draine, ne journalise ni ne signale les jobs restants dans la `LinkedBlockingDeque` au moment de la suppression. Comportement conforme aux Dev Notes de la story (§ Teardown de file à la suppression, qui ne couvre explicitement que le job en cours d'exécution) ; aucune AC ne requiert cette traçabilité. Amélioration UX possible (avertir l'admin du nombre de jobs perdus) dans une story dédiée si le besoin émerge en usage réel.
- **`unregisterPrinter()` n'est pas synchronisé avec le commit de la transaction de `delete()`** — `pluribourse-backend/src/main/java/org/pluribourse/domain/print/service/PrinterService.java` (`delete`) — Le handle est retiré de la map en mémoire de façon synchrone dans la méthode `@Transactional`, avant que Hibernate ne flush/commit réellement la suppression de la ligne en base (flush différé par défaut). Une requête `GET /admin/printers` concurrente peut donc, dans une fenêtre étroite, voir encore la ligne en base alors que son handle a déjà disparu de la map. Un null-check défensif a été ajouté dans `list()` (patch appliqué lors de cette revue) pour neutraliser le symptôme observable (NPE → 500) ; une correction complète nécessiterait une synchronisation transactionnelle (`TransactionSynchronizationManager.registerSynchronization(...afterCommit...)`), jugée disproportionnée pour ce risque à fenêtre très étroite. Aucun pattern équivalent n'existe ailleurs dans le code.

## Deferred from: code review of story-3.10 (2026-07-22)

- **Imports wildcard dans `UpdateLotDto`/`UpdateLotItemDto`** — `pluribourse-backend/src/main/java/org/pluribourse/domain/item/dto/UpdateLotDto.java` — vs imports explicites dans les autres nouveaux fichiers du même diff. Nit de style pur, inoffensif.
- **Duplication de quelques lignes d'affectation de champs** — `LotService.java` (boucles `noChangeIndexes`/`lockOrder`) — au lieu d'un helper partagé. Nit DRY mineur. Le volet frontend équivalent (`lot-form.component.ts`, blocs de mapping create/update) a été résolu le 2026-08-19 (méthode `toItemPayload()` partagée) ; le volet backend n'a pas été touché lors de cette même passe — factoriser `noChangeIndexes`/`lockOrder` toucherait à l'ordre de verrouillage documenté en commentaire, jugé trop sensible pour un quick-win.
- ~~**`resetForm()` supprimé, corps dupliqué à deux endroits**~~ → ✅ Résolu 2026-08-19 : méthode privée `resetForm()` réintroduite dans `lot-form.component.ts`, appelée aux deux endroits.
- ~~**Arrondi monétaire dupliqué**~~ → ✅ Résolu 2026-08-19 : `globalPrice` calculé une seule fois dans `onSubmit()` et réutilisé pour les deux branches create/update.
- **Pas de test pour la borne haute de 50 articles ni les limites de longueur nom/commentaire sur le chemin update** — `LotManagementIT.java` — lacune symétrique déjà présente (et jamais testée) sur le chemin `create()` pré-existant, pas une régression.
- **`TooManyItemsException` inatteignable/non testée via le chemin update** — `LotService.java` — même lacune pré-existante, non testée à l'échelle du projet, que pour `create()`.
- ~~**`PUT`/`DELETE` concurrents sur le même lot pourraient lever une `ObjectOptimisticLockingFailureException` non gérée (500 brut)**~~ → ✅ Résolu 2026-08-19 : même handler global (voir item Story 3.2 ci-dessus), couvre `Lot` sans code spécifique.
- ~~**`startEdit()` ne remet pas `depositMode` à `'individual'`**~~ → ✅ Résolu 2026-08-19 : `this.depositMode.set('individual')` ajouté dans `startEdit()`, `deposit-page.component.ts`.
- **`startEditLot()` retourne silencieusement sans toast si les articles du lot ne sont plus dans la liste déjà chargée** (ex. suppression concurrente dans une autre session) — `deposit-page.component.ts` — course très rare, aucune AC ne l'exige.
- **`confirmDeleteLot()` ne recharge pas la liste ni ne réinitialise `editingLot` sur une erreur non-422** (ex. 404 si le lot a déjà été supprimé ailleurs) — `deposit-page.component.ts` — reproduit exactement le même pattern pré-existant déjà utilisé par `confirmDelete()` pour les articles individuels.
- **Pas de garde anti-double-clic sur le nouveau bouton de suppression de lot** (un double-clic rapide pourrait déclencher deux `DELETE`, le second recevant un 404 et affichant une erreur après une suppression pourtant réussie) — `deposit-page.component.html` — reproduit la même lacune pré-existante sur le bouton de suppression d'article individuel (`confirmDelete()`).
- **Le cas 404 d'`onSubmit()` de `LotFormComponent`** (lot ou article n'existe plus, ex. suppression concurrente) **tombe dans la branche d'erreur générique** — `lot-form.component.ts` — le formulaire reste ouvert en référençant une entité obsolète, la liste parente n'est pas rafraîchie. Course de suppression concurrente rare, aucune AC ne l'exige.

## Deferred from: re-review of story-3.8 (2026-07-22)

- ~~**`DELETE /admin/printers/{id}` concurrent sur le même id peut lever une `ObjectOptimisticLockingFailureException` non mappée**~~ → ✅ Résolu 2026-08-19 : même handler global (voir item Story 3.2 ci-dessus). Renvoie 409, pas le 404 idempotent un temps envisagé — choix délibéré : un handler générique unique ne peut pas distinguer une suppression concurrente (où 404 serait plus naturel) d'un conflit de mise à jour (où 409 est correct) ; 409 reste sémantiquement correct dans les deux cas.
- **`DELETE /admin/printers/{id}` avec un id non numérique renvoie le 400 Spring par défaut au lieu d'un `ProblemDetail` structuré** — `pluribourse-backend/src/main/java/org/pluribourse/domain/print/controller/PrinterController.java` — Comportement Spring MVC standard sur tout `@PathVariable Long id` mal formé (`MethodArgumentTypeMismatchException` non mappée dans `GlobalExceptionHandler`). Systémique à tous les contrôleurs de l'application utilisant ce pattern, pas introduit par cette story.

## Deferred from: re-review of story-3.10 (2026-07-22)

- **Pas de verrouillage de ligne sur `Lot`/`Item` pendant `update()`/`delete()`** — `pluribourse-backend/src/main/java/org/pluribourse/domain/item/service/LotService.java` — Deux `PUT` concurrents, ou un `PUT` concurrent à un `DELETE`, sur le même lot lisent-puis-écrivent l'état Item/Lot (occupation des tables via `assignTable()`, réconciliation des membres) sans verrou pessimiste sur la ligne Lot (seul le vendeur est verrouillé, et seulement conditionnellement). Cohérent avec le pattern pré-existant de `ItemService.update()` (cité explicitement en Dev Notes comme précédent pour ne pas verrouiller sur un changement de catégorie seul). Aucune AC n'exige de garantie de cohérence plus forte ici. Note 2026-08-19 : le volet « `ObjectOptimisticLockingFailureException` non gérée » évoqué à l'origine dans ce point est résolu (handler global, voir Story 3.2 ci-dessus) — reste ouvert uniquement l'absence de verrou pessimiste elle-même.
- **`PhaseGuard` potentiellement périmé avant le commit de la transaction (TOCTOU sur la phase de l'édition)** — `pluribourse-backend/src/main/java/org/pluribourse/domain/item/service/LotService.java` — `PhaseGuard.requireDepositPhase()` n'est vérifié qu'une fois en début de méthode ; si la phase de l'édition avance en cours de transaction, la mutation peut tout de même committer après la fin effective du Dépôt. Même schéma que tous les autres points d'appel de `PhaseGuard` dans le code (`ItemService`, etc.), non introduit par cette story.
- **Pattern de suppression incohérent et appels `.save()` redondants dans `LotService.update()`** — La boucle de retrait des membres absents appelle `itemRepository.delete(member)` article par article, alors que `delete()` quelques lignes plus loin utilise `itemRepository.deleteAll(...)` pour le cas équivalent en masse. La boucle `noChangeIndexes` appelle aussi `itemRepository.save(item)` sur des entités déjà suivies par le contexte de persistance, où le dirty-checking d'Hibernate flusherait de toute façon au commit. Même calibre que les nits DRY déjà différés ailleurs dans cette story.
- **Boutons Modifier/Supprimer le lot non conditionnés par `isDepositPhase()` dans le template** — `pluribourse-frontend/src/app/features/volunteer/deposit/deposit-page.component.html` — Les nouveaux boutons d'action lot se trouvent dans le même bloc `article-row__actions` que les boutons Modifier/Supprimer d'article individuel pré-existants, dont aucun n'est encadré par un `@if (isDepositPhase())` — les deux restent visibles et cliquables hors phase Dépôt, échouant seulement après un aller-retour réseau avec un toast 422. Lacune UX pré-existante que les nouveaux boutons lot prolongent de façon cohérente, pas une régression introduite par cette story.
- **Assertions non-null sur des champs `lotName`/`lotPrice` indépendamment nullables** — `item.lotName!` (template) et `first.lotName!`/`first.lotPrice!` (`startEditLot()`) affirment la non-nullité de champs typés `string | null`/`number | null` sur `ItemDto` — l'invariant "toujours renseignés ensemble quand `lotId` est présent" n'est garanti que par convention côté backend, pas par le système de types. Non atteignable en pratique avec le mapping backend actuel, mais à durcir un jour (ex. union discriminée) plutôt que d'affirmer.
- **Ids de base de données internes intégrés au texte `detail` des erreurs** — `pluribourse-backend/src/main/java/org/pluribourse/domain/item/exception/LotNotFoundException.java`, `DuplicateLotItemException.java` — Le message renvoyé au client inclut l'id numérique brut ("Lot not found: " + id). Cohérent avec la convention déjà utilisée pour `ItemNotFoundException` à l'échelle du projet, pas un nouveau pattern.
- **`confirmDeleteLot()` capture `selectedSeller()` avant la fin de l'appel de suppression** — `pluribourse-frontend/src/app/features/volunteer/deposit/deposit-page.component.ts` — `seller` est lu juste après la résolution du dialog de confirmation mais avant `await firstValueFrom(this.lotService.delete(lotId))`. Si le bénévole change de vendeur pendant que cette requête est en vol, le chemin de succès recharge la liste pour le vendeur capturé au moment de la confirmation, pas celui actuellement sélectionné. Course rare, aucune AC ne l'exige — même sévérité que les autres cas déjà différés dans cette story.

## Deferred from: code review of story-3.11 + story-3.12 (2026-07-27)

_Revue conjointe (les deux stories ont été implémentées et testées ensemble dans la même session)._

- **Pas de garde si `printerBridgeClient.discover()` renvoie `null` ou si le champ `type` d'une imprimante découverte est `null`** — `pluribourse-backend/src/main/java/org/pluribourse/domain/print/service/PrinterService.java:74-83` (`discover()`/`toDiscoveredPrinterDto`) — `.stream()` sur un `null` lèverait une NPE ; un `type` `null` est silencieusement mappé vers `A4` plutôt que rejeté. Risque faible : le contrat PrinterBridge (`PrinterBridge/CLAUDE.md`) garantit un tableau JSON avec des champs renseignés sur `GET /printers`. À corriger si PrinterBridge se montre non fiable en pratique.
- **Aucun test ne fait passer un vrai aller-retour WebSocket réussi/ERROR par le code réel de `PrinterBridgeClient.print()`/`sendAndAwaitResult()`/`ResultCapturingHandler`** — `pluribourse-backend/src/test/java/org/pluribourse/domain/print/service/PrinterBridgeClientTest.java` — seul le cas "connexion refusée" (port 1) est testé sur ce chemin réel ; le cas de succès et le cas `PrintResult.status()=="ERROR"` ne sont couverts que via un `PrinterBridgeClient` mocké dans `DepositSlipPrintingIT`, qui n'exerce jamais `sendAndAwaitResult`/`ResultCapturingHandler` eux-mêmes. Gap déjà documenté consciemment dans les Completion Notes des deux stories (double de test WebSocket jugé disproportionné par rapport à un simple `HttpServer` JDK). À revisiter si les bugs de fuite de session/thread (patch appliqué ou non lors de cette revue) ou de mauvaise classification des erreurs WS (decision-needed, voir story 3.11/3.12) motivent d'investir dans un double WS capable.

## Deferred from: code review of story-3.13 (2026-07-28)

- **Imprimante ignorée enregistrable directement via `POST /admin/printers` sans passer par la réactivation** — `pluribourse-backend/src/main/java/org/pluribourse/domain/print/service/PrinterService.java` — Laisserait une ligne `ignored_printers` orpheline, mais sans impact fonctionnel réel : `discover()` exclurait de toute façon ce `printerBridgeId` via le check "déjà enregistrée". Aucun chemin de l'interface actuelle ne permet ce cas (le picker de découverte exclut déjà les imprimantes ignorées). Risque théorique, non actionnable sans une story dédiée à la réconciliation des deux registres.
- **`ignoredAt` stocké en `LocalDate` (pas d'heure), aucun tri explicite sur `listIgnored()`/`findAll()`** — `pluribourse-backend/src/main/java/org/pluribourse/domain/print/entity/IgnoredPrinter.java`, `service/PrinterService.java` — L'ordre d'affichage de la section "Imprimantes ignorées" est arbitraire (ordre de retour JPA). Non requis par l'AC ; à adresser si des retours utilisateurs sur le tri remontent.
- **`@PathVariable String printerBridgeId` non validé en longueur face au `VARCHAR(32)` de l'entité** — `pluribourse-backend/src/main/java/org/pluribourse/domain/print/controller/PrinterController.java` — Un id surdimensionné (via un appel API direct, pas via l'UI normale) produirait une erreur de contrainte DB brute (500) plutôt qu'un 400 propre. Aucun précédent de validation de path-variable n'existe ailleurs dans ce contrôleur ; risque faible (endpoint ADMIN uniquement).
- **Toast générique unique pour tout échec ignorer/réactiver** — `pluribourse-frontend/src/app/features/admin/printers/printer-form.component.ts`, `printer-list.component.ts` — 422 (déjà enregistrée), 404 (introuvable) et 5xx/réseau produisent tous le même message générique côté frontend, perdant la distinction que le backend encode via le statut HTTP et le `ProblemDetail`. Amélioration UX possible, non requise par l'AC.
- ~~**Aucun message d'état vide dans la vue liste du dialog de création quand `discoveredPrinters()` est vide**~~ → ✅ Résolu 2026-08-19 : message `admin.printers.create.empty` affiché à la place du tableau vide.

## Deferred from: re-review of story-3.13 (2026-07-28)

- **Catch `DataIntegrityViolationException` dans `ignore()` trop large** — `pluribourse-backend/src/main/java/org/pluribourse/domain/print/service/PrinterService.java` — Pourrait masquer une autre violation de contrainte (ex. un `printerBridgeId` dépassant `VARCHAR(32)`) comme un faux succès (204) plutôt que la race de duplication spécifiquement visée. Le resserrer proprement nécessiterait une introspection d'exception spécifique au SGBD (SQLState/nom de contrainte), fragile entre H2 (tests) et MariaDB (prod), pour un cas très marginal : l'id vient uniquement de PrinterBridge (contrat interne), jamais d'une saisie utilisateur directe.
- ~~**Pas de `scope="col"` sur les nouveaux `<th>`**~~ → ✅ Résolu 2026-08-19 : `scope="col"` ajouté sur les 3 tables (`printer-form.component.html`, les 2 tables de `printer-list.component.html`). Gap d'accessibilité systémique ailleurs dans l'application (autres tables du projet) toujours présent, hors périmètre de ce fix ponctuel.
- **Découverte importante lors de la 2ᵉ revue, déjà corrigée** (mentionnée ici pour traçabilité, pas un item à traiter) : le test d'idempotence séquentiel (Order 14) ne testait pas réellement la protection anti-race ajoutée au 1er tour de revue — le second appel court-circuitait avant d'atteindre le `catch`. Un vrai test de concurrence (Order 21, deux threads) a révélé que `@Transactional` sur `ignore()` faisait planter le second appel avec `UnexpectedRollbackException` (Hibernate invalide la session après un échec de flush, même catché en Java). Corrigé en retirant `@Transactional` de la méthode. Vérifié stable sur 5 exécutions consécutives.

## Deferred from: code review of story-6.1 (2026-07-29)

- **`ItemCatalogPageDto` sérialise directement `Page<T>` de Spring Data** — `pluribourse-backend/src/main/java/org/pluribourse/domain/item/dto/ItemCatalogPageDto.java` — Structure JSON non garantie stable entre versions Spring (avertissement déjà visible dans les logs de test : "Serializing PageImpl instances as-is is not supported"). Pattern préexistant, déjà utilisé par `AdminSellerController` avant cette story — pas une régression introduite ici. À traiter globalement (migration vers `PagedModel`/`@EnableSpringDataWebSupport(pageSerializationMode = VIA_DTO)`) si Spring Data change son format de sérialisation dans une future montée de version.

## Deferred from: code review of story-4-2-gestion-du-panier-validation-du-paiement (2026-07-31)

- **Pas de test de concurrence multi-thread réel pour la course de création du panier (`createBasket`)** — `pluribourse-backend/src/main/java/org/pluribourse/domain/pos/service/PosBasketService.java:151-164` — Le mécanisme (catch `DataIntegrityViolationException` + re-résolution) n'est exercé que par un scénario séquentiel (`PosBasketIT`, deux appels successifs), jamais par une vraie course concurrente. Audit 2026-08-19 : la Story 4.4 (livrée) couvre une race différente (verrou optimiste `Item` côté `validate()`), jamais la contrainte unique de `createBasket()` elle-même. Toujours à couvrir si le besoin se confirme.

## Deferred from: code review of story-4-3-gestion-des-lots-au-pos (2026-08-13)

- **Race TOCTOU dans `PosBasketService.removeLot`** — `pluribourse-backend/src/main/java/org/pluribourse/domain/pos/service/PosBasketService.java:111-120` — `findAllByBasketIdAndItemLotId` puis `deleteAll` ne sont pas atomiques : deux appels `removeLot` concurrents sur le même lot pourraient tous deux lire une liste non vide avant que l'un des deux ne committe, le second renvoyant alors 200 (suppression idempotente, 0 ligne réellement affectée) au lieu du 404 attendu. État final toujours correct (le lot est bien retiré), pas de corruption de données. Même catégorie de risque que la sécurité de la concurrence multi-postes explicitement reportée à la Story 4.4 (cf. defers Story 4.1/4.2 ci-dessus).
- **Logique de filtrage lot/hors-lot dans le template Angular plutôt que précalculée dans le composant** — `pluribourse-frontend/src/app/features/volunteer/pos/pos-page.component.html:17-63` — Deux `@for` imbriqués sur `basket()!.items` (un par groupe de lot + un pour les articles hors-lot) au lieu d'un `computed()` précalculant les listes filtrées dans le composant. O(items × groupes), poursuit le patron déjà en place depuis la Story 4.2 pour le rendu des articles hors-lot. Acceptable à l'échelle actuelle (paniers de quelques articles, 1-2 lots distincts max par panier, cf. Dev Notes § performance de la story). À adresser si les paniers/lots grossissent significativement.
- ~~**Bouton « Retirer le lot entier » sans binding `[disabled]` lié à `removeInFlight`**~~ → ✅ Résolu 2026-08-19 : `removeInFlight` converti en signal public et bindé en `[disabled]` sur le bouton. Le bouton de retrait d'article individuel (même garde côté TS, même gap visuel) n'a pas été touché — hors périmètre de ce fix ponctuel, à traiter ensemble si un refacto plus large des boutons du panier est fait.
- **Structure du commentaire `last_updated` dans `sprint-status.yaml`** — `_bmad-output/implementation-artifacts/sprint-status.yaml:38` — Ligne unique croissante couvrant plusieurs stories (4-2, 6-1, 4-1, 4-3...), sans séparation structurelle, rendant l'historique difficile à auditer programmatiquement à mesure qu'il grossit. Convention déjà en place dans tout le fichier avant cette story ; question de process documentaire indépendante du code.

## Deferred from: code review of 4-4-securite-de-la-concurrence-multi-postes (2026-08-13)

- **Branche `JpaSystemException`/`SnapshotIsolationException` non exercée en panier multi-articles** — `pluribourse-backend/src/main/java/org/pluribourse/domain/pos/service/PosBasketService.java:169-186` — `SaleConcurrencyIT` ne met qu'un seul article par panier ; le comportement de la boucle `for (Item item : items)` quand un item plus tôt a déjà réussi puis un item suivant entre en conflit (MariaDB ou H2) n'est prouvé par aucun test. Préexistant : la branche `ObjectOptimisticLockingFailureException` d'origine (Story 4.2) a la même limite. **Enrichi round 3** (Acceptance Auditor) : la boucle réutilise la même `Session`/`EntityManager` Hibernate pour flusher l'item suivant après qu'un item précédent a échoué au flush — la spec JPA (§3.4) documente ce réemploi post-échec comme non garanti. Risque latent réel pour un panier multi-articles en conflit partiel, toujours préexistant (Story 4.2) et hors périmètre de l'AC 3 (l'absence de vente partielle reste assurée quel que soit le comportement de la boucle, puisque toute la transaction est annulée dans tous les cas). Corriger proprement impliquerait un arbitrage de conception (arrêter la boucle au premier échec vs. accepter le risque documenté) hors périmètre de la Story 4.4.
- **Aucun pipeline CI n'exécute `SaleConcurrencyIT` après merge** — `pluribourse-backend/src/test/java/org/pluribourse/domain/pos/SaleConcurrencyIT.java` — Contrainte projet documentée et acceptée (`architecture.md` : « CI/CD : Aucun pour la v1 »). Sans CI, ce test ne s'exécute que sur les machines de développeurs ayant Docker lancé localement.
- **`GlobalExceptionHandler` sans handler générique pour `JpaSystemException`** — `pluribourse-backend/src/main/java/org/pluribourse/shared/exception/GlobalExceptionHandler.java` — Toute `JpaSystemException` non reconnue (y compris la branche "rethrow" ajoutée par cette story, ou tout autre appel ailleurs dans l'app) tombe dans le comportement d'erreur par défaut de Spring Boot plutôt que dans le contrat RFC7807 utilisé partout ailleurs. Préexistant, non introduit par cette story.

## Deferred from: code review of 4-4-securite-de-la-concurrence-multi-postes — round 2 re-review (2026-08-13)

- **Aucun test unitaire rapide dédié pour `isCausedBy`** — `pluribourse-backend/src/main/java/org/pluribourse/domain/pos/service/PosBasketService.java:264-274` — Cette exacte logique a déjà causé une régression réelle plus tôt dans ce même cycle de revue (première tentative `NestedExceptionUtils.getMostSpecificCause` boguée, détectée seulement via un run Testcontainers de 30s). Un test unitaire trivial (chaîne d'exceptions factice, appel direct de `isCausedBy`) l'aurait détectée en millisecondes. En tension avec la philosophie de test du projet (« pas de tests de service isolés », CLAUDE.md) : nécessiterait une décision de conception (visibilité/extraction de `isCausedBy`) plutôt qu'un correctif trivial.
- **Le test ne distingue pas, en boîte noire, laquelle des deux branches `catch` a effectivement intercepté la course** — `pluribourse-backend/src/test/java/org/pluribourse/domain/pos/SaleConcurrencyIT.java` — Vérifié empiriquement non-applicable en l'état actuel (les logs `mvn test` confirment à chaque run que c'est la nouvelle branche `JpaSystemException`/`SnapshotIsolationException` qui est exercée, pas l'ancienne `ObjectOptimisticLockingFailureException`). Risque réel pour l'avenir seulement : si un futur changement de version Hibernate/driver faisait redevenir `ObjectOptimisticLockingFailureException` le chemin emprunté, le test resterait vert sans qu'on s'en aperçoive. Adresser ça nécessiterait de tester un détail d'implémentation interne plutôt que le contrat observable — pas souhaitable.

## Deferred from: code review of story 4-5 (impression de la facture acheteur) (2026-08-13)

- **Race TOCTOU entre `PrintQueueService.isAvailable()` et `.submit()` dans `PosInvoicePrintService`** — `pluribourse-backend/src/main/java/org/pluribourse/domain/pos/service/PosInvoicePrintService.java` — Préexistante depuis `DepositValidationService.reprintDepositSlip`/`reprintLabels` (Story 3.5/3.6), reproduite fidèlement ici comme demandé par les Dev Notes de la story elle-même (« même mécanisme que la réimpression du bordereau de dépôt »). Une file suspendue ou une imprimante retirée entre le check et le submit n'est pas re-vérifiée de façon atomique. Non introduite ni aggravée par cette story.
- **Variante de la même race : suppression d'imprimante entre le check et `submit()` fait fuiter un 404 au lieu d'un 422** — `pluribourse-backend/src/main/java/org/pluribourse/domain/pos/service/PosInvoicePrintService.java`, `pluribourse-backend/src/main/java/org/pluribourse/domain/print/service/PrintQueueService.java:63-69` — Si un admin supprime l'imprimante A4 entre `isAvailable()` et `submit()`, `PrintQueueService.submit` lève `PrinterNotFoundException` (404) au lieu de la `InvalidPrinterSelectionException` (422) attendue par le frontend — le message d'erreur dédié `a4PrinterUnavailable` ne s'affiche pas dans cette fenêtre de course étroite. Même cause racine que le point précédent, préexistante depuis le même mécanisme d'origine (Story 3.5/3.6).
- **Scoping par `saleId` de `ItemRepository.findAllBySaleIdOrderById` non prouvé par un test avec une seconde vente distincte** — `pluribourse-backend/src/test/java/org/pluribourse/domain/print/InvoicePrintingIT.java` — La classe ne crée qu'une seule `Sale` sur toute l'édition ; une régression future de la clause `WHERE i.sale.id = :saleId` (ex. retournant tous les items de l'édition) passerait inaperçue par les assertions actuelles, car elles coïncident avec le seul jeu de données existant. Nécessiterait une seconde vente/vendeur non liés pour être détecté.
- **Verrou anti-double-clic `printingInvoice()` jamais exercé par un test d'appels concurrents réels** — `pluribourse-frontend/src/app/features/volunteer/pos/pos-page.component.spec.ts` — Le test de réimpression attend la résolution du premier appel (`await`) avant de démarrer le second, donc la branche `if (this.printingInvoice() || !sale) return;` n'est jamais exercée avec `printingInvoice()===true`. Même lacune déjà acceptée pour `scanInFlight`/`removeInFlight`/`validateInFlight` ailleurs dans ce même composant depuis les Stories 4.2/4.3, pas spécifique à cette story.

## Deferred from: code review of story 2-8 (annulation du panier lors d'une transition de phase — côté serveur) (2026-08-13)

- **Race TOCTOU : un panier créé pendant la fenêtre de transition pourrait échapper à l'annulation** — `pluribourse-backend/src/main/java/org/pluribourse/domain/edition/service/EditionService.java:154-165` — Entre le `SELECT` (`findAllByEditionId`) et le commit de la transaction de transition de phase, un bénévole dont la requête `getOrCreateCurrentBasket` s'exécute en concurrence verrait encore l'ancienne phase (READ_COMMITTED, l'UPDATE de phase de l'admin n'est pas encore visible) et pourrait légitimement créer un nouveau panier qui ne serait jamais capturé par le `SELECT` déjà exécuté — ce panier survivrait alors à la transition. Fenêtre de course très étroite (une transition de phase admin est une action rare, pas un chemin chaud). Même classe de problème que la concurrence déjà traitée comme un effort dédié par la Story 4.4 (Testcontainers MariaDB) — hors périmètre de la Story 2.8.
- **Race TOCTOU : une vente validée concurremment pourrait déclencher un `basket-cancelled` trompeur** — `pluribourse-backend/src/main/java/org/pluribourse/domain/edition/service/EditionService.java:159-165` — Si `PosBasketService.validate()` supprime un panier entre le `SELECT` et le `deleteAll` de la transition de phase, le second `DELETE` est un no-op (0 ligne affectée, aucune corruption), mais l'événement SSE `basket-cancelled` est quand même diffusé pour un panier qui a en réalité été vendu avec succès, pas annulé. Faible sévérité (payload informatif inexact, aucune perte de données), fenêtre de course étroite.
- **`EditionService` dépend désormais directement du package `pos` (`BasketRepository`)** — `pluribourse-backend/src/main/java/org/pluribourse/domain/edition/service/EditionService.java` — Dépendance cross-domaine (le domaine edition accède directement au repository POS) cohérente avec le précédent déjà établi dans la même classe (`ItemRepository`, utilisé depuis `deleteEdition`) — pas un problème nouveau introduit par cette story, mais une piste de nettoyage de frontière de domaine à considérer si le besoin se généralise.
- **`basketRepository.deleteAll(activeBaskets)` génère N `DELETE` individuels plutôt qu'une requête bulk** — `pluribourse-backend/src/main/java/org/pluribourse/domain/edition/service/EditionService.java:163-165` — Spring Data JPA n'optimise pas `deleteAll(Iterable)` en un `DELETE ... WHERE id IN (...)` unique. Négligeable à l'échelle du projet (NFR-001 : ~3 postes caissiers simultanés, donc au plus une poignée de paniers actifs) ; une requête `@Modifying` bulk serait l'optimisation naturelle si le volume grandissait un jour.

## Deferred from: code review of story-4-6-gestion-du-changement-de-phase-dans-le-composant-pos-cote-client (2026-08-14)

- **Deux connexions SSE simultanées vers `/api/sse/events`** — `pluribourse-frontend/src/app/services/sse.service.ts:38-72` — `PosPageComponent.basketCancelled()` et `AppLayoutComponent.phaseChanges()` ouvrent chacun leur propre `EventSource` ; l'extraction `listen<T>()` de cette story partage le code de connexion mais pas la connexion elle-même. Un bénévole sur `/volunteer/pos` maintient donc deux flux SSE ouverts en parallèle. Comportement préexistant (chaque appel à `phaseChanges()` ouvrait déjà sa propre connexion avant cette story) ; cette story l'étend à un second flux sans l'introduire. Mutualiser nécessiterait un multicast partagé (`shareReplay({refCount:true})` ou équivalent) — hors périmètre, aucun AC ne le demande.
- **Requêtes HTTP en vol non annulées à l'arrivée de `basket-cancelled`** — `pluribourse-frontend/src/app/features/volunteer/pos/pos-page.component.ts:81-133` — `addItem`/`removeItem`/`removeLot` en vol au moment de l'annulation ne sont pas abortées (pas d'`AbortSignal`/`takeUntil`), seule leur réponse est ignorée côté client une fois arrivée. Impact réel faible : le panier est déjà supprimé côté serveur par la même transition de phase (Story 2.8), donc la requête échoue généralement en 404 sans dommage. Piste d'amélioration réelle mais non bloquante.
- **`scannerDisabled` se réinitialise sur une navigation SPA interne, pas seulement sur un rechargement complet** — `pluribourse-frontend/src/app/features/volunteer/pos/pos-page.component.ts:49-51` — Le commentaire du code (et l'AC 2 de la story) affirment que seul un rechargement complet de page réactive le composant. En réalité, quitter `/volunteer/pos` puis y revenir en navigation interne Angular détruit et recrée `PosPageComponent` (stratégie de réutilisation de route par défaut), ce qui réinitialise `scannerDisabled` à `false` sans rechargement complet. Aucun préjudice fonctionnel identifié : `loadBasket()` échouerait immédiatement contre la nouvelle phase via `PhaseGuard` côté serveur, laissant `basket()` à `null` et donc le scanner toujours inopérant via les gardes `!currentBasket` déjà en place. Changer ce comportement nécessiterait de retoucher l'AC 2 elle-même (décision produit), hors périmètre d'une revue de code. Audit 2026-08-19 : le signal a depuis été renommé `basketCancelled`, comportement inchangé.

## Deferred from: code review of story-5-1-flux-de-solde-des-vendeurs (2026-08-14)

- **Aucune trace de l'utilisateur ayant réalisé un solde** — `pluribourse-backend/src/main/java/org/pluribourse/domain/payout/entity/Settlement.java` — `Settlement` porte `status`/`amount`/`settledAt` mais aucune référence à l'acteur (bénévole ou admin) qui a déclenché `settle`/`markUnclaimed`, contrairement à `Sale` qui porte `user`. Aucun AC de la Story 5.1 ne l'exige — à considérer si une story de rapports/audit future en a besoin.

## Deferred from: code review of story-5-2-generation-du-bilan-de-vente-pdf (2026-08-14)

- **TOCTOU non-atomique `isAvailable`/`submit`** — `pluribourse-backend/src/main/java/org/pluribourse/domain/payout/service/SettlementReportPrintService.java` — Entre la vérification de disponibilité de l'imprimante A4 et la soumission du job, un autre thread pourrait la rendre indisponible. Même dette déjà actée pour `PosInvoicePrintService`/4.5 et les services jumeaux du même module — pas nouvelle à cette story.
- **`SettlementReportPrintingIT` Order 10 ne distingue pas une vraie `LazyInitializationException` cross-thread d'un simple échec WebSocket du double de test** — `pluribourse-backend/src/test/java/org/pluribourse/domain/print/SettlementReportPrintingIT.java` — Limitation connue de `PrinterBridgeDouble` (ne complète pas le handshake WebSocket), déjà présente pour `InvoicePrintingIT`/`DepositSlipPrintingIT`. Les seuls tests qui vérifient réellement le contenu du PDF s'exécutent sur le même thread, dans la transaction `@Transactional(readOnly = true)` — ils ne peuvent structurellement pas reproduire un échec de lazy-loading cross-thread.
- **`DocumentPrintService.buildSettlementReportJob` capture `SellerProfile` entier dans la closure du `PrintJob`** plutôt que d'en extraire des valeurs simples avant la construction — `pluribourse-backend/src/main/java/org/pluribourse/domain/print/service/DocumentPrintService.java` — Reproduit sciemment, et documenté dans la story, le patron déjà validé en production par `DepositSlipRenderer` depuis la Story 3.6 (le renderer ne touche que `getFirstName()`/`getLastName()`/`getEdition().getName()`) — pas un risque nouveau introduit ici, mais toujours non couvert par un test dédié qui le prouverait explicitement.
- **Aucun scénario testé pour un vendeur sans aucun article vendu ni invendu** (rapport à deux tableaux vides) — `pluribourse-backend/src/test/java/org/pluribourse/domain/print/SettlementReportPrintingIT.java` — Cas limite réel mais mineur, comportement attendu (deux tableaux vides, net à 0,00€) jamais démontré par un test.

## Deferred from: code review of story-5.3 (2026-08-18)

- **`DailyReportRenderer.renderDailyReport` ne capture que `DocumentException`** — `pluribourse-backend/src/main/java/org/pluribourse/domain/print/service/DailyReportRenderer.java:90-92` — Une `NoSuchMessageException` levée par `messageSource.getMessage` (clé i18n manquante pour une locale) se propagerait sans appeler `document.close()`. Ce patron try/catch (pas de catch générique, pas de try-with-resources sur `Document`) est déjà celui des 3 autres renderers existants (`InvoiceRenderer`, `DepositSlipRenderer`, `SettlementReportRenderer`) — pré-existant, non introduit par cette story. Les 12 nouvelles clés `print.dailyReport.*` sont bien présentes dans `messages_fr.properties` et `messages_en.properties`, donc le risque ne se matérialise pas aujourd'hui. À adresser globalement (les 4 renderers) si une clé i18n venait un jour à manquer.

## Deferred from: code review of story-5.4 (2026-08-18)

- **`AdminReportController.getEditionReport` résout l'édition et calcule le rapport dans deux transactions `readOnly` séparées (TOCTOU étroit sur la phase)** — `pluribourse-backend/src/main/java/org/pluribourse/domain/report/controller/AdminReportController.java:42-43` — `requireEdition(editionId)` puis `getEditionReport(edition)` sont deux appels transactionnels distincts sur un endpoint GET ; la phase pourrait changer entre les deux. Patron identique déjà présent sur le endpoint GET du bilan journalier (Story 5.3 : `getActiveEdition()` puis `getDailyReport()`) — pas nouveau, impact réel négligeable pour un endpoint de lecture (au pire un rapport reflète une phase d'une fraction de seconde périmée, se corrige au prochain rafraîchissement).
- **Prolifération des sous-classes `*NotAllowedException`** — `pluribourse-backend/src/main/java/org/pluribourse/domain/item/exception/EditionReportNotAllowedException.java` — 6 sous-classes désormais (`DepositReprintNotAllowedException`, `ItemModificationNotAllowedException`, `SettlementNotAllowedException`, `EditionReportNotAllowedException`, `SellerDeletionNotAllowedException`, `SellerManagementNotAllowedException`) plutôt qu'une exception paramétrée unique. Convention déjà établie dans tout le code base, cette story s'y conforme, ne l'introduit pas. À reconsidérer si le nombre continue de croître.
- **`EditionReportRenderer` duplique quasi intégralement le boilerplate de `DailyReportRenderer`** — `pluribourse-backend/src/main/java/org/pluribourse/domain/print/service/EditionReportRenderer.java` — Polices, `NO_COMPRESSION`, construction de table copiées plutôt que partagées via une base commune. Patron déjà présent sur les 5 renderers PDF du projet (`DepositSlipRenderer`/`InvoiceRenderer`/`SettlementReportRenderer`/`DailyReportRenderer`/`EditionReportRenderer`), explicitement demandé par les Tasks de cette story. Une extraction de base commune serait une story de refactoring à part, pas un fix ponctuel.
- **`EditionSummaryReportPrintService` duplique le boilerplate de `DailySalesReportPrintService`** — `pluribourse-backend/src/main/java/org/pluribourse/domain/report/service/EditionSummaryReportPrintService.java` — Résolution imprimante/vérification disponibilité/soumission job identiques entre les deux classes. Même patron déjà présent sur tous les services d'impression de rapports/bilans du projet, pas introduit ici.
- **Branche `default -> throw new IllegalStateException(...)` du `switch` `PaymentMethod` non testée dans `getEditionReport`** — `pluribourse-backend/src/main/java/org/pluribourse/domain/report/service/ReportService.java:89` — Ajoutée pour éviter qu'un futur moyen de paiement disparaisse silencieusement des recettes, mais aucun test ne l'exerce. Lacune identique déjà acceptée sur la même branche de `getDailyReport` (Story 5.3) — à traiter ensemble si jamais adressée.

## Deferred from: code review of story-5.5 (2026-08-19)

- **Duplication du calcul du montant dû entre `getAssociationRetainedTotal` et `getSettlementsForEdition`** — `pluribourse-backend/src/main/java/org/pluribourse/domain/payout/service/SettlementService.java:78-89` — Deux implémentations indépendantes de la même formule (`ItemPricing.computeTotal` + `computeNetPayout` par vendeur) au lieu de réutiliser `SettlementDto.amountDue`. Conforme au bloc de code prescrit explicitement par la story 5.5 (décision actée avec l'utilisateur le 2026-08-19) — pas actionnable sans revoir cette décision.
- **Prix vide sur les lignes d'article de lot dans l'export catalogue** — `pluribourse-backend/src/main/java/org/pluribourse/domain/report/service/ReportExportService.java:188-189` — `item.price()` est `null` par conception pour un article de lot (le prix réel vit sur `Lot.globalPrice`, jamais exposé par `ItemCatalogDto`). Confirmé identique au comportement déjà en place sur l'écran catalogue existant (`pluribourse-frontend/src/app/features/catalog/item-catalog.component.html` affiche aussi `item.price` brut sans repli sur le prix de lot) — préexistant, non introduit par cette story.
- **Redirection `password-change-required` cassée pour les réponses `responseType: 'blob'`** — `pluribourse-frontend/src/app/core/interceptors/auth.interceptor.ts:17` et `pluribourse-frontend/src/app/shared/http-error.util.ts:7-9` — `extractErrorType()` lit `error.error.type` en le castant en corps JSON ; pour les deux nouveaux endpoints d'export (première utilisation de `responseType: 'blob'` du projet), `error.error` est un `Blob` réel et `.type` retourne son type MIME, pas le champ `type` du `ProblemDetail`. Un 403 `password-change-required` déclenché pendant un export tombe dans la branche générique (déconnexion + `/login`) au lieu de `/change-password`. Cas de bord étroit (nécessite `forcePasswordChange=true` ET un clic sur un bouton d'export) qui s'autocorrige au login suivant via le garde `authGuard`, qui vérifie `forcePasswordChange` indépendamment de cet appel.
